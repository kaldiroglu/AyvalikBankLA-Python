services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ayvalikbank
      POSTGRES_USER: bank
      POSTGRES_PASSWORD: bank
    ports:
      - "5435:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
